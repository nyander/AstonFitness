/*function map(arr,f){
	var result = [];
	for(var i = 0;i<arr.length;i++){
		var nextElement = f(arr[i]);
		result.push(nextElement);
	}
	return result;
}

function filter(arr,condition){
	var result = [];
	for(var i = 0;i<arr.length;i++){
		if(condition(arr[i])){
			result.push(arr[i]);
		}
	}
	return result;
}

function reduce(arr,f,acc){
	for(var i = 0;i<arr.length;i++){
		acc = f(acc,arr[i]);
	}
	return acc;
}*/